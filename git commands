# first contribution to a repo

# fork to the repo
git clone https://github.com/Asyfundyar/first-contributions.git

# create a new branch
git checkout -b add-asfandyar-khan

# make changes to a file using a text editor or IDE

# check status if you want, it will show you which file has been modified
git status

# adding changes to the branch
git add Contributors.md

# committing the changes
git commit -m "Add asfandyar-khan to Contributors list"

# push changes
git push origin add-asfandyar-khan


# other commands

git config --global user.name "Asfandyar Khan"
 
git config --global user.email gulshierkhan@gmail.com
 
code // to open vscode
 
git config --global core.editor "code --wait" // set vscode as default editor
 
git config --global -e // to open config file

git config --global core.autocrlf input

git config --help // detailed help

git config -h // help summary
